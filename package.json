{
  "name": "react-circleci",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "serve-build": "pushstate-server ./build 3000",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "test-cypress": "yarn build && run-p --race serve-build cypress"
  },
  "devDependencies": {
    "cypress": "^2.0.4",
    "pushstate-server": "^3.0.1"
  }
}
